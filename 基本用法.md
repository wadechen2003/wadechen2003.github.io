# 基本用法

``` 
String jsonSchemaPath = "some Schema.json Path" ;  
JsonNode fstabSchema = JsonLoader.fromPath(jsonSchemaPath);
JsonSchemaFactory factory = JsonSchemaFactory.byDefault();
JsonSchema schema = factory.getJsonSchema(fstabSchema);
ProcessingReport report;
report = schema.validate(jsonObj);
```




